CREATE TABLE users (
id INTEGER PRIMARY KEY AUTOINCREMENT,
name TEXT NOT NULL
);

CREATE TABLE messages (
name TEXT,
timestamp INTEGER,
message TEXT,
room TEXT,
FOREIGN KEY (name) REFERENCES users(name));